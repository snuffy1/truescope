version: "3.8"

services:
  truescope:
    build:
      context: .
      dockerfile: Dockerfile
    image: truescope:latest
    container_name: truescope
    ports:
      - "3007:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always